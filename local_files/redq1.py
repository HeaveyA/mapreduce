#!/usr/bin/env python
import sys

for line in sys.stdin:
    line = line.strip()

    def reduce(key, values[])
        sum, count = 0
        for value in values:
            sum += value
            count += 1
        emit(sum / count)